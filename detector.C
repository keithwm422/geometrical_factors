// This code is a MC to evaluate the geometrical factor or "effective area"
// of a given detector geometry. 
// First, get back that for a planar detector that observes only half the sky

#include <iostream>
#include <fstream> 

using namespace std;


void detector(){
  //generate the detector positions

  // generate the random trajectory of the particle

  // if the particle hits the dectector, record

  // if particle does not, skip

  // divide the two?
  



}
